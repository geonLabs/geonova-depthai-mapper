# Legacy CLI adapter config. Prefer postprocess/main.py sync.
dataset: ../../data/REPLACE_RUN_ID_raw
output_dir: ../../data/processed/REPLACE_RUN_ID/synced
sync_threshold_ms: 50.0
rgb_depth_threshold_ms: 10.0
copy_images: true
allow_legacy: false
